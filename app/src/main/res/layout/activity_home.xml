<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/main"
    tools:context=".telaprincipal.HomeActivity"
    android:background="@color/azul_escuro"
    >

        <!-- Logo -->
        <ImageView
            android:id="@+id/logoImage"
            android:layout_width="wrap_content"
            android:layout_height="249dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_marginStart="90dp"
            android:layout_marginTop="100dp"
            android:layout_marginEnd="90dp"
            android:src="@drawable/ic_logo"
            android:padding="8dp"/>

        <!-- Boas-vindas -->

    <!-- Descrição do app -->
    <TextView
        android:id="@+id/textBoasVindas"
        android:layout_width="275dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/logoImage"
        android:layout_alignStart="@id/logoImage"
        android:layout_alignEnd="@id/logoImage"
        android:layout_marginStart="-24dp"
        android:layout_marginTop="17dp"
        android:layout_marginEnd="-20dp"
        android:fontFamily="@font/montserrat"
        android:text="Bem-vindo!"
        android:textAlignment="center"
        android:textColor="@color/white"
        android:textSize="24sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="148dp"
        android:layout_above="@+id/bottomNavigation"
        android:layout_below="@id/textBoasVindas"
        android:layout_alignStart="@id/textBoasVindas"
        android:layout_alignEnd="@id/textBoasVindas"
        android:layout_marginStart="-46dp"
        android:layout_marginTop="15dp"
        android:layout_marginEnd="-43dp"
        android:layout_marginBottom="40dp"
        android:fontFamily="@font/montserrat_light"
        android:text="Explore e colabore com a Plataforma Colaborativa de Monitoramento de Objetos Plásticos. Este aplicativo georreferenciado permite que você registre ocorrências de poluição plástica utilizando seu celular."
        android:textAlignment="center"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:padding="8dp"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="48dp"
        android:layout_marginTop="30dp"
        android:layout_marginEnd="48dp"
        android:layout_marginBottom="30dp"
        android:background="@drawable/bottom_background"
        android:elevation="2dp"
        app:itemIconSize="25dp"
        app:itemIconTint="@drawable/item_selector"
        app:itemRippleColor="@android:color/transparent"
        app:labelVisibilityMode="unlabeled"
        app:menu="@menu/bottom_menu" />

</RelativeLayout>